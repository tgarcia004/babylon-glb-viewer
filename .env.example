# Optional override for the hosting server URL (local dev parallel server).
# VITE_HOST_SERVER_URL=http://127.0.0.1:3847

# Published GitHub Pages host-models URL (set automatically in deploy workflow).
# VITE_GITHUB_HOST_MODELS_URL=https://tgarcia004.github.io/babylon-glb-viewer/host-models

# Dev host server only — fake remote latency (ms) and region label for testing.
# HOST_SIMULATE_LATENCY_MS=180
# HOST_SIMULATE_REGION=US West (simulated)
