/** Stop the browser from opening/navigating to a dropped file (handled in modelFileDrop.ts). */
export function initPreventBrowserFileDrop(): void {
  // modelFileDrop registers capture-phase drop; nothing else required here.
}
